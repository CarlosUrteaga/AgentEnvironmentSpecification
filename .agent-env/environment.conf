# Agent Environment Specification. Values are literal single-line text.
AES_VERSION=1
PROJECT_NAME=Agent Environment Specification
PURPOSE=Provide a repeatable stack-neutral workspace bootstrap for coding agents
AUDIENCE=Software engineers using coding agents for new or existing repositories
FIRST_MILESTONE=Ship a portable Bash CLI that initializes validates renders and diagnoses an agent-ready repository
TECH_CONSTRAINTS=Bash 3.2 or newer standard Unix tools and Git on macOS and Linux
SETUP_COMMAND=No setup required
TEST_COMMAND=./tests/run.sh
LINT_COMMAND=bash -n agent-env tests/run.sh
ALLOWED_ACTIONS=Read repository files; edit task-related files; run syntax checks and integration tests
RESTRICTED_PATHS=Secrets; production credentials; files outside the selected target repository
SUCCESS_CRITERIA=The CLI safely generates repeatable managed agent instructions and all integration tests pass
MODULE_POLICY=true
MODULE_EVALS=true
MODULE_CLAUDE=false
