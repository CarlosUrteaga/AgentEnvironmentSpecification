# Example contract for an AI engineering interview project.
AES_VERSION=1
PROJECT_NAME=Weather Logistics API
PURPOSE=Provide weather-aware delivery estimates for logistics operations
AUDIENCE=Dispatchers and systems integrating delivery planning
FIRST_MILESTONE=Expose health weather lookup and delivery estimate endpoints with tests
TECH_CONSTRAINTS=Choose the smallest interview-appropriate API stack and document the decision
SETUP_COMMAND=unknown
TEST_COMMAND=unknown
LINT_COMMAND=unknown
ALLOWED_ACTIONS=Create application code and tests; add required dependencies; run local checks
RESTRICTED_PATHS=Secrets; real customer data; deployment actions; paid external APIs without approval
SUCCESS_CRITERIA=Documented endpoints validate inputs return deterministic errors and pass automated tests
MODULE_POLICY=true
MODULE_EVALS=true
MODULE_CLAUDE=false
